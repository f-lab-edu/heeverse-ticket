<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heeverse.concert.domain.mapper.ConcertMapper">

  <insert id="insertConcert" parameterType="com.heeverse.concert.domain.entity.Concert">
    INSERT INTO concert (artistId, venueId, concertName, concertDate, ticketOpenTime, ticketEndTime)
    VALUES (#{artistId}, #{venueId}, #{concertName}, #{concertDate}, #{ticketOpenTime}, #{ticketEndTime});
  </insert>
</mapper>